# mongo-vector-backend

Project scaffold for a Python backend library (managed with `uv`) that will interact with MongoDB Atlas Vector Search.

## Requirements

- Python >= 3.12
- `uv`

## Setup

```powershell
uv sync --extra dev
```
